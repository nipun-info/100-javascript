
// 21.6: Math, Abs, Round, Ceil, Floor and Random Number 

/**
 * 
 * 

const Math = {
    min: function min(num1, num2){
        return num2;
    }
}

// console.log(Math.min);
// console.log(Math.max);

const min = Math.min(45, 21, 65, 99, 126, -99);
console.log(min);
const max  = Math.max(45, 21, 65, 99, 126, -99);
console.log(max);

// Math.abs():Pi: This is the ratio of the circumference of a circle to its diameter.
console.log(Math.PI); 

// Math.abs(): An intrinsic object that provides basic mathematics functionality and constants
console.log(Math.abs(5-10));
console.log(Math.abs(10-5));


// Math.round(): Returns a supplied numeric expression rounded to the nearest integer.
console.log(Math.round(2.25));
console.log(Math.round(2.65));

// Math.floor():Returns the greatest integer less than or equal to its numeric argument.
console.log(Math.floor(2.65));
console.log(Math.floor(2.25));

// Math.ceil():Returns the smallest integer greater than or equal to its numeric argument.
console.log(Math.ceil(2.25));
console.log(Math.ceil(2.65));

// Math.random():Returns a pseudorandom number between 0 and 1.
console.log(Math.random());
console.log(Math.random() * 10);

console.log(Math.round(Math.random() * 10));

*/ 